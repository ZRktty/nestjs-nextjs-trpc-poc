{
  "name": "nestjs-nextjs-trpc-poc",
  "description": "A minimal full-stack proof-of-concept demonstrating end-to-end type safety across a NestJS backend and Next.js frontend using tRPC — no REST, no manual type duplication, just a single shared type contract from server to UI.",
  "private": false,
  "scripts": {
    "generate": "pnpm --filter backend generate",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.10.0",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@10.11.0"
}
